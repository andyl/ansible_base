---

# User Keys:
# Fetch key
# Copy key to GitHub
# Copy key to fileserver
#
# Note that the pubkey is in gen_user_pubkey

- name: Fetch ssh pubkey
  fetch:
    src: "{{tgt_keyfile}}"
    dest: "{{lcl_keyfile}}"
    flat: yes

# - name: Copy pubkey to GitHub
#   local_action: command gh_keycopy {{lcl_keyfile}}

- name: Fucking fuck
  local_action: command whoami

- name: Copy pubkey to {{filehost}}
  local_action: command /home/aleak/util/code_util/bin/keycopy {{gen_user}}@{{filehost}} -f {{lcl_keyfile}}

