---

# AsdfElixir

- name: install asdf_elixir
  command: "bash -ilc '.asdf/bin/asdf install elixir {{ item }}'"
  args:
    creates: "/home/{{gen_user}}/.asdf/installs/elixir/{{ item }}"
    chdir: "/home/{{ gen_user }}"
  with_items:
    - 1.9.2
  become_user: "{{ gen_user }}"

# - name: set global elixir version
#   command: "bash -lc 'asdf global elixir {{ elixir_vsn }}'"
#   args:
#     chdir: "/home/{{ gen_user }}"
#   become_user: "{{ gen_user }}"
