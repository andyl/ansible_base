---
 
# Vars/env

- debug:
    msg: "RUNNING DOTENV"

- name: convert dotenv variables
  local_action: command whoami
  # failed_when: no
  # changed_when: no

- name: load dotenv variables
  become: false
  include_vars: 
    file: /tmp/ansible_dotenv.yaml

