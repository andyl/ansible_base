---

# vscode editor
 
- name: create pkg dir
  file: 
    path: /home/{{tgt_user}}/.pkg 
    state: directory 
    group: "{{tgt_user}}"
    owner: "{{tgt_user}}"

- name: get deb file
  get_url: 
    url:  http://{{cblob_host}}/vscode/code_{{vscode_version}}.deb
    dest: /home/{{tgt_user}}/.pkg/code_{{vscode_version}}.deb
    group: "{{tgt_user}}"
    owner: "{{tgt_user}}"

- name: install package
  apt: 
    deb: /home/{{tgt_user}}/.pkg/code_{{vscode_version}}.deb

