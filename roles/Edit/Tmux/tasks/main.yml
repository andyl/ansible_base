---

# NOTE: tmux.conf is in ~/util/base_util/cfg/_tmux.conf

- name: install tmux executable
  get_url:
    url:  http://{{cblob_host}}/tmux/tmux-{{host_arch}}-{{tmux_vsn}}
    dest: /usr/bin/tmux-{{tmux_vsn}}
    mode: "a+rx"
  become: true

- name: create tmux symlink 
  file: 
    src:   /usr/bin/tmux-{{tmux_vsn}}
    dest:  /usr/bin/tmux
    state: link
    force: yes
  become: true

# tmate 
# https://tmate.io
# https://github.com/tmate-io/tmate
# https://github.com/tmate-io/tmate/releases

- name: install tmate executable
  get_url:
    url:  http://{{cblob_host}}/tmux/tmate-{{tmate_vsn}}
    dest: /usr/bin/tmate-{{tmate_vsn}}
    mode: "a+rx"
  become: true
  when: is_x86_64

- name: create tmate symlink 
  file: 
    src:   /usr/bin/tmate-{{tmate_vsn}}
    dest:  /usr/bin/tmate
    state: link
    force: yes
  become: true
  when: is_x86_64

