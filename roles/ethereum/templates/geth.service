#! /usr/bin/env bash

# {{ansible_managed}}
# {{template_path}}

# geth

[Unit]
Description=Ethereum/Geth

[Service]
Type=simple
ExecStart=/usr/bin/geth daemon
ExecStop=/usr/bin/pkill geth
Restart=always
User={{rem_user}}

[Install]
WantedBy=default.target
