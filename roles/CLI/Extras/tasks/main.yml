---

# Extras

- name: create pkg dir
  file: 
    path: /home/{{gen_user}}/.pkg 
    state: directory 
    group: "{{gen_user}}"
    owner: "{{gen_user}}"

- name: extras for x86_64
  include: inc_x86_64.yml
  when: is_x86_64

- name: extras for arm7
  include: inc_arm7.yml
  when: is_arm7
