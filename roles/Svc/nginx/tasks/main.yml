---

# nginx

- name: nginx base configuration
  include: base.yml

- name: debug yeah
  debug:
    msg: "{{nginx_cfgs}}"

# - name: nginx blob configuration
#   include: blob.yml
#   when: nginx_blob
#
# - name: nginx alt configuration
#   include: blob.yml
#   when: nginx_alt

