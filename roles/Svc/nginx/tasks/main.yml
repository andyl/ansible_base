---

# nginx

- name: nginx base configuration
  include: base.yml

- debug:
    var: nginx_cfgs

# - name: nginx blob configuration
#   include: blob.yml
#   when: nginx_blob
#
# - name: nginx alt configuration
#   include: blob.yml
#   when: nginx_alt

